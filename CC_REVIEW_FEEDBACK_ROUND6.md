# Hyht 実装プランへの第6回フィードバック

## 差分確認

同じプランファイルの内容が更新されています。第5回フィードバックの主な指摘である、タイムラインpolicyテストの条件分離が反映されました。

- 通常更新で300件打ち切り: `.atEnd`
- 期限以外のモード境界を繰り上げ収録: `.atEnd`
- 期限をdoneとして繰り上げ収録: `.never`
- 途中でdoneへ到達: `.never`

これにより、第5回時点の残課題は解消されています。

## レビュー結果

**レビュー通過。実装開始可能です。**

設計上の新しい阻害事項はありません。追加の全面レビューは不要です。

## 軽微な修正

### タイムラインテスト件数の表記を固定値にしない

プラン本文ではpolicyの4条件を個別テストへ分離したため、列挙されたケースを個別に数えると次の11ケースになります。

1. 総エントリー数300件以下
2. 日時が昇順・重複なし
3. 現在時刻をカバー
4. モード変更後の間隔切替
5. 通常更新打ち切りの`.atEnd`
6. モード境界繰り上げの`.atEnd`
7. 期限繰り上げの`.never`
8. 途中doneの`.never`
9. モード境界の繰り上げ収録
10. 期限の繰り上げ収録
11. 繰り上げ時の昇順・重複なし・300件以内

一方、フェーズ8と受入テストには「タイムライン生成器の自動テスト9項目」とあります。実装時のテスト関数の分割方法でも件数は変わるため、次の表現へ変更してください。

- 変更前: `タイムライン生成器の自動テスト9項目通過`
- 変更後: `上記タイムライン生成器の全テストケース通過`

これは編集上の修正であり、実装開始前の再レビューは不要です。

## 実装時の方針

- 現在の実装プランを正とする
- 上記の件数表記だけ修正して実装へ進む
- フェーズ1から順番に実施する
- 各フェーズの完了条件とテストを満たしてから次へ進む
- 実際に成功したXcodeGen・xcodebuildコマンドをREADMEへ記録する
- iOS 17 runtimeがない場合は最低対応OS検証を未確認事項として報告する

## 結論

プラン改訂は確認済みです。件数表記を直した後、そのまま実装を開始してください。
