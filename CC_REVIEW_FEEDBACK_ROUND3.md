# Hyht 実装プランへの第3回フィードバック

v3はほぼ実装可能です。実装開始前に以下を修正してください。

## 必須修正

### 1. JavaScript `toFixed`の期待値を訂正する

プラン記載の`9.995→10.00`はJavaScriptの実際の結果と一致しません。

```js
(9.995).toFixed(2) // "9.99"
```

次のように修正してください。

- 例示を`9.995→9.99`へ訂正する
- 手書きの期待値を正とせず、Node.jsで実行してfixtureを生成する
- Swift実装は生成済みfixtureと照合する
- `1.005`、`2.675`、`9.995`、末尾ゼロ、桁上がり、実際のカウントダウンで現れる代表値を含める
- fixture生成に使用したNode.jsスクリプトもリポジトリへ含め、再生成可能にする

### 2. タイムラインのreload policyを修正する

300件到達時に「301件目の日時」を`.after(date)`へ指定すると、300件目から301件目まで再生成が要求されません。301件目が期限やモード境界の場合、切替が新しいタイムラインの生成待ちになります。

前回フィードバックの「301件目を`.after`へ指定」という提案も訂正します。

次の仕様へ変更してください。

- 300件到達時は`.atEnd`を使用する
- または`.after(lastIncludedEntry.date)`を使用する
- 301件目に相当する候補日時は、連続性の検証用に計算するだけでreload日時には使用しない
- 最終収録エントリーがdoneの場合は`.never`

自動テストを次のように修正してください。

- 300件目から次タイムラインの最初のエントリーまで、通常更新または境界更新の欠落がない
- 300件目の次候補がモード境界の場合も、境界より前に再生成が要求される
- 300件目の次候補が期限の場合も、期限より前に再生成が要求される
- doneを収録済みの場合は`.never`

### 3. App Groupの読み書き責務を統一する

プランではWidgetを読み取り専用とする一方、フェーズ1完了条件が「両ターゲットから読み書き可」になっています。

次へ統一してください。

- アプリ本体: `state.json`の書き込み、破損ファイル退避、デフォルト再作成を担当
- Widget: 読み取りのみ。decode失敗時は安全なデフォルト表示を行い、ファイルを変更しない
- 未知schemaVersion時もWidgetはファイルを変更しない
- アプリ内の書き込みはActorまたはシリアルキューで直列化する
- 書き込みは`Data.write(options: .atomic)`を使用し、Widgetは旧版または新版の完全なDataを読む

プロセス内のシリアルキューはアプリとWidget間の排他制御にはならないため、「`NSFileCoordinator`またはシリアルキュー」という同等扱いをやめてください。初版では次の構成を推奨します。

- アプリ内の多重書き込み防止: Actor
- プロセス間の読み取り整合性: atomic write
- Widget: `Data(contentsOf:)`による読み取りのみ

フェーズ1の完了条件は「アプリから書き込み、Widgetから同じ内容を読み取れる」へ変更してください。

## 追加修正

### 最低対応OSの検証

iPhone 16 SimulatorではiOS 17を実行できないため、最新OSだけではiOS 17+の最低対応確認になりません。

検証環境を次のように分けてください。

- 最新OS: iPhone 16 Simulator
- 最低対応OS: iOS 17.xを実行可能なiPhone 15以前のSimulator

iOS 17 Simulator runtimeが必要であることを前提条件または手動受入条件へ記載してください。

### タイムゾーンIDの用途

期限をUTC絶対時刻で保存し、別途タイムゾーンIDを保持する目的を明記してください。推奨仕様は次の通りです。

- 新規作成時に端末のタイムゾーンIDを保存する
- DatePickerと日時表示には保存済みタイムゾーンを使用する
- ユーザーが別タイムゾーンへ移動しても期限の絶対時刻と表示上の現地設定時刻を変えない
- 初版ではタイムゾーン変更UIを提供しない

### ModePolicyのコード内fallback

「ModePolicyをコードへ埋め込まない」と「JSON読込失敗時はコード内デフォルトへfallback」が矛盾しています。

安全用の例外としてコード内fallbackを残す場合は、次を明記してください。

- 正本はversion付きModePolicy JSON
- コード内定義はリソース欠損時だけ使う安全用fallback
- JSONとコード内fallbackが意味的に一致する自動テストを追加する

### 不要なgitignore設定

`state.corrupt-*.json`はApp Groupコンテナに作成され、通常はリポジトリ内に存在しません。`.gitignore`への追加は不要なので削除してください。

## 修正後の判断基準

以下を満たせば実装開始可能です。

- JavaScriptで生成した正しいtoFixed fixtureを使用する
- 300件目の後、301件目より前にタイムライン再生成が要求される
- アプリだけがApp Groupファイルを変更し、Widgetは読み取り専用である
- 最新OSと最低対応iOS 17の検証環境が区別されている
- タイムゾーンIDとModePolicy fallbackの意味が明確である
