# Hyht 実装プランへの第5回フィードバック

## 判定

**レビュー通過。実装開始可能です。**

第4回フィードバックの以下はすべて反映されています。

- `.atEnd`の実行時刻がOS依存であること
- 301件目相当が重要境界・期限の場合の繰り上げ収録
- OSの再生成時刻をユニットテスト対象にしないこと
- `hyht://edit`の`edit`をhostとして扱うこと
- 破損退避ファイル名を安全な日時形式にすること

設計全体の追加改訂は不要です。

## 実装時の軽微な注意

### タイムラインpolicyテストの条件を分ける

「300件打ち切り時のpolicyが`.atEnd`」というテストは、301件目相当が期限でdoneを繰り上げ収録する場合には`.never`となります。テスト名と条件を次のように分けてください。

- 通常更新で300件打ち切り: `.atEnd`
- 期限以外のモード境界を繰り上げ収録: `.atEnd`
- 期限をdoneとして繰り上げ収録: `.never`
- 途中でdoneへ到達: `.never`

これはプランの設計変更ではなく、テスト実装時の条件整理です。

### フェーズ1で検証コマンドを実測確定する

ローカルSwiftPM packageのscheme公開状況により、`-workspace`、`-project`、scheme名が変わる可能性があります。フェーズ1で次を実行し、実際に成功したコマンドだけをREADMEへ記録してください。

```bash
xcodebuild -list -project Hyht.xcodeproj
xcodebuild -list -workspace Hyht.xcodeproj/project.xcworkspace
```

### 実装範囲を維持する

- 初版対象外の機能を追加しない
- 各フェーズの完了条件とテストを満たしてから次へ進む
- iOS 17 runtimeが未導入なら、最低対応OS検証を未確認事項として明示する
- XcodeGen未導入時は、インストール前にユーザーへ確認する

## 結論

追加のプランレビューは不要です。このv4を正として実装へ進んでください。
