# Hyht 実装プランへの第4回フィードバック

v4は実装開始可能な水準です。必須事項は反映されています。実装前に、タイムラインの`.atEnd`に関する表現とテストだけ修正してください。

## 修正事項

### `.atEnd`は次候補より前の実行を保証しない

現プランには、`.atEnd`により「300件目より後・301件目より前に再生成が要求されるため切替の取りこぼしがない」とあります。

`.atEnd`は最終エントリー消化後にWidgetKitへ新しいタイムラインを要求させる方針ですが、実際のprovider呼び出し時刻はOS管理です。301件目相当の日時より前に必ず実行される保証はありません。この点は、同プランの「WidgetKitの実行はベストエフォート」という記述とも揃えてください。

次のように修正してください。

- 300件到達時は`.atEnd`を返す
- 説明は「最終収録エントリー消化後に再生成を要求する」に留める
- 「301件目より前に必ず再生成される」「切替の取りこぼしがない」という保証表現は削除する
- 実際のprovider呼び出し時刻はOS依存と明記する

### 301件目が重要境界の場合は現在のタイムラインへ収録する

301件目相当の次候補がモード境界または期限の場合、`.atEnd`による再生成だけに依存すると、OS側の遅延によって切替が遅れる可能性があります。

次の処理を追加してください。

1. 300件到達時に301件目相当の次候補を計算する
2. 次候補が通常更新なら、そのまま300件＋`.atEnd`
3. 次候補がモード境界または期限なら、最後の通常更新エントリー1件を除外し、その境界エントリーを300件目として収録する
4. 収録した境界が期限なら`.never`
5. 収録した境界が期限以外なら`.atEnd`

これにより、重要な状態切替を将来エントリーとして事前登録し、provider再実行の遅延に依存しにくくできます。

## テスト修正

OSの再生成時刻は純粋関数のユニットテストでは検証できません。現在の以下のテスト表現を変更してください。

- 変更前: 「境界より前に再生成が要求される」
- 変更後: 「300件打ち切り時のpolicyが`.atEnd`である」
- 追加: 「301件目相当がモード境界なら、その境界が300件以内へ繰り上げ収録される」
- 追加: 「301件目相当が期限なら、doneが300件以内へ繰り上げ収録されpolicyが`.never`になる」
- 追加: 「繰り上げ時も日時が昇順、重複なし、総数300件以内である」

Simulatorまたは実機では、タイムライン更新がOS依存であることを前提に、境界前後の表示を手動確認してください。

## 軽微な実装注意

- `hyht://edit`の`edit`はURLのpathではなくhostとして解釈されるため、ルーティング条件を`url.host == "edit"`として明記すると実装迷いがありません。
- 破損退避ファイル名の日時は、ファイル名として安全な形式（例: `yyyyMMdd-HHmmssSSS`）を使うと扱いやすくなります。

## 判定

上記は局所修正であり、設計全体の再レビューは不要です。反映後、そのまま実装開始可能です。
