# Hyht 実装プランへの第2回フィードバック

前回の主要指摘は反映されています。実装開始前に、以下を修正してください。

## 必須修正

### 1. タイムライン生成を総数300件以内にする

現プランは通常エントリーを最大300件生成した後、最大5件の境界エントリーを追加するため、総数が300件を超えます。また、現在時刻の即時表示、分境界への整列、途中のモード変更、重複排除が未定義です。

生成手順を次のアルゴリズムへ変更してください。

1. 現在時刻のエントリーを必ず最初に追加する
2. 現在のモードから次の通常更新日時を計算する
3. 次の通常更新日時と次のモード境界日時を比較する
4. 早い方を追加し、その日時でモードを再計算する
5. 同一日時のエントリーを重複させず、総数300件まで繰り返す
6. 301件目に相当する次候補日時を `.after(date)` に指定する
7. doneへ到達した場合はdoneエントリーを追加して終了し、`.never` を指定する

clock/minでは、最初の現在時刻エントリーは未整列でも許容し、2件目以降を時計の分境界（秒=0）へ整列してください。

タイムライン生成器には以下の自動テストを追加してください。

- 総エントリー数が常に300件以下
- 日時が昇順かつ重複なし
- 現在時刻をカバーするエントリーが存在する
- モード変更後に更新間隔が切り替わる
- 300件目と次回再生成日時の間に更新漏れがない
- 期限を含む場合、最後がdoneかつpolicyが`.never`

### 2. 「5分以内」を製品保証にしない

WidgetKitの再読み込み日時はOSが管理するため、設定変更や期限到達後の反映を5分以内と保証することはできません。また、week=1時間、day=15分、hour=6分という更新間隔とも矛盾します。

仕様と受入条件を次のように分けてください。

- 設定保存時: 即時に`reloadTimelines(ofKind:)`を要求するが、実際の反映時刻はOS依存
- weekの設計上の更新間隔: 1時間
- dayの設計上の更新間隔: 15分
- hourの設計上の更新間隔: 6分
- clock/min: 1分エントリー。実際の反映はベストエフォート
- Simulatorでの「5分以内」は試験時の目安とし、製品保証・合否条件にはしない

### 3. 未知schemaVersionと自動保存の衝突を解消する

現プランは未知schemaVersionを上書きしない一方、編集画面では自動保存します。未知schema検出時の扱いを次のように定義してください。

- Repositoryは元のraw Dataをそのまま保持する
- アプリはデフォルト表示へフォールバックする
- 編集、初期化、自動保存を無効化する
- 「新しいバージョンで作成されたデータ」と表示する
- Widgetは安全なデフォルト表示を行う
- 読み込み後も保存済みraw Dataがbyte単位で変化しないテストを追加する

破損データについても、復旧時に元データを退避するか、そのまま破棄するかを明記してください。

### 4. 自動保存による過剰なWidget reloadを防ぐ

現状の記述では、イベント名などを1文字入力するたびに保存とタイムライン再読み込みが実行されます。保存Coordinatorへ以下を追加してください。

- 300〜500ms程度のdebounce
- 同一状態は保存しない
- 保存処理を直列化する
- 古い保留処理を破棄し、最新状態だけ保存する
- 保存成功後、Widget表示へ影響する変更の場合だけ1回reloadする
- アプリが非アクティブになる際に保留中の保存をflushする
- 保存失敗時は「保存済み」と表示しない

### 5. テストターゲット・scheme・コマンドを確定する

「HyhtUITests（必要なら）」「scheme名はフェーズ1で確定」「`-destination ...`」が残っており、完全版としては未確定です。

初版では次の構成に確定してください。

- `HyhtCoreTests`: SwiftPM Test Target
- UI Test Target: 初版では作成せず、UIは手動受入テスト
- XcodeGen生成後に利用する共有schemeを明記する
- 実際に実行可能な`xcodebuild test`と`xcodebuild build`コマンドを記載する
- 固定機種を使う場合は必要なSimulatorを前提条件へ記載する。固定しない場合は利用可能なSimulatorを選択する手順を記載する

## 追加修正

### JavaScript `toFixed`互換

- `String(format:)`を使う場合は`en_US_POSIX`等へlocaleを固定する
- 「一般的な四捨五入」ではなく、JavaScript `Number.prototype.toFixed`との一致を仕様とする
- JavaScript側で生成した期待値fixtureを用意し、Swift実装と照合する
- `.005`近傍、末尾ゼロ、桁上がりを含める

### CountdownModePolicy

初版UIで編集不可の固定ポリシーを`AppState`へ保存すると、破損・改変・マイグレーション対象が増えます。コードへ埋め込まない要件は、version付きの同梱JSONで満たし、Repositoryのユーザーデータには含めない構成を推奨します。

保存対象とする場合は、バリデーション、不正時の既定ポリシー復旧、version移行を定義してください。

### App Group保存方式

UserDefaultsまたはApp Group内JSONファイルのどちらを使うか確定してください。少なくとも以下を定義してください。

- 保存場所またはUserDefaults key
- 書き込みの原子性
- アプリとWidgetの同時アクセス
- decode失敗時の扱い
- 未知schemaVersionのraw Data保持方法

### 対象外との整合

初版ではiCloud同期が対象外なので、「iCloud実装へ差し替え可能」をフェーズ6の完了条件から削除してください。Repositoryによる保存先の抽象化までで十分です。

### プレビューの完了条件

OSのレンダリング、壁紙、ティント処理までアプリ内プレビューと完全一致させることはできません。「プレビュー＝実ウィジェットの見た目一致」を、次へ変更してください。

- アプリ内プレビューとWidgetは同じ共通Viewを使用する
- 内容、配置、フォントサイズが一致する
- OS固有の背景・ティント差は実機またはSimulatorで別途確認する

## 修正後の判断基準

以下を満たせば実装開始可能です。

- タイムラインが総数300件以内で連続生成される
- WidgetKitのベストエフォート動作とアプリ側の更新間隔が区別されている
- 未知schemaVersionが自動保存で破壊されない
- 自動保存がWidget reloadを連発しない
- テスト構成と検証コマンドが確定している
