# プレビュー編集モード切替

## 仕様

- 「完了状態」のトグルを廃止する。
- 同じ位置に「通常／完了」のセグメントを配置する。
- 通常ではイベント名・絵文字・期限を編集する。
- 完了では表示文字・完了絵文字・共通の期限を編集する。
- プレビューとテンプレートカードは選択中のモードへ連動する。
- 選択状態は画面内だけで保持し、保存形式は変更しない。

## 確認

- 初期選択が通常であること。
- 切替時にフォーム、プレビュー、テンプレートカードが同期すること。
