# 保存状態フィードバックの簡素化

## 仕様

- 自動保存の通常状態ではメッセージを表示しない。
- 保存中の表示も行わない。
- 保存に失敗した場合だけ、テンプレートセクションのフッターにエラーを表示する。
- 保存処理と状態管理は変更しない。

## 確認

- `idle`、`saving`、`saved`で表示が空になること。
- `failed`で既存のエラー表示が維持されること。
